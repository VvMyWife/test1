# platform-foundation

`platform-foundation` 是一个 **纯 Python** 的能力库，供 `backend/services/*` 复用。

约束：

- 不依赖 FastAPI / SQLAlchemy / Celery
- 只包含可复用的 operators / pipeline / inference adapters / rules engine / contracts
